<form method="post" action="{{ path('analitics_delete', {'id': analitic.id,
                                                            'project_id':project.id}) }}" onsubmit="return confirm('Вы уверены что хотите удалить этот отчет?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ analitic.id) }}">
    <button class="btn">Delete</button>
</form>
