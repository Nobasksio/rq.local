{% extends 'work.html.twig' %}

{% block title %}Аналитика{% endblock %}

{% block body %}
    <div class='zag1'>
        Аналитика
    </div>
    <div class='pzag'>
        Тут будут хранится все пакетные аналитические отчеты вашего старого меню. <br>
        Вы можете посмотреть готовые или создать новые
    </div>
<table class='table_order' border='0' cellpadding='0'  cellspacing='0' >
    <tr class='th_order_table'>
        <td class='tdz_ord_table'>Название</td>
        <td class='tdz_ord_table'>Начало периода</td>
        <td class='tdz_ord_table'>Конец периода</td>
        <td class='tdz_ord_table'>Дата создания</td>
        <td class='tdz_ord_table'></td>
        <td class='tdz_ord_table'></td>
    </tr>
    {% for analitic in analitics %}
        <tr class='tr_order' id='tr{{ analitic.id }}' >
            <td class='td_ord_table'  onclick="location.href ='{{ path('analitics_show',{'id':analitic.id,
                'project_id':project.id}) }}'">{{ analitic.name }}</td>
            <td class='td_ord_table' onclick="location.href ='{{ path('analitics_show',{'id':analitic.id,
                'project_id':project.id}) }}'">{{ analitic.dateStart|date("d.m.Y") }}</td>
            <td class='td_ord_table' onclick="location.href ='{{ path('analitics_show',{'id':analitic.id,
                'project_id':project.id}) }}'">{{ analitic.dateFinish|date("d.m.Y") }}</td>
            <td class='td_ord_table' onclick="location.href ='{{ path('analitics_show',{'id':analitic.id,
                'project_id':project.id}) }}'">{{ analitic.dateCreate|date("d.m.Y") }}</td>
            <td class='td_ord_table'><span class='redact_grey_smal' onclick = 'hide_analitics(this,{{ project.id }},{{ analitic.id }})'>удалить</span></td>
            <td class='td_ord_table'><a href="{{ path('analitics_edit', {'id': analitic.id,
                    'project_id':project.id}) }}">ред.</a></td>
        </tr>
    {% endfor %}
</table>
    <a href='{{ path('analitics_new',{'project_id':project.id}) }}'>
        <div class='buton'>
            Создать новый отчет
        </div>
    </a>
{% endblock %}
